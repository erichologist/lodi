>I am making a tool in ai studio, I'd like the tool to generate images like those listed under "Examples of GSAP in Action". my current prompt is: `Create a mobile prioritized tool that provides an image thumbnail for inputted URLs and allows users to customize thumbnail size and format (for example, markdown, html, etc.). Also include options for image quality and watermarking. Additionally, implement drag-and-drop functionality for easy reordering of thumbnails. Allow users to save their customized thumbnail settings for future use. and export them in various formats. Allow for batch input and output of multiple URLs simultaneously. Also, add a feature for resizing images before generating thumbnails. Make the interface dark mode with customizable color themes, and prioritized for mobile devices.` can you help me write a prompt that best conveys what I want the output to produce?


That’s a great idea for a tool! The reason your current prompt might feel a bit "clunky" is that it’s asking the AI to build the entire application logic (drag-and-drop, database saving, batch processing) in one go, rather than focusing on the visual output of those "Examples of GSAP in Action" previews.
To get that premium, high-end "GSAP showcase" look, you need a prompt that emphasizes layout, spacing, and modern UI trends like the glassmorphism and animations we just discussed.
Here is a refined version of your prompt:
______
## The Refined Prompt

"Act as a Senior UI/UX Designer. Generate a high-fidelity, mobile-first dashboard UI for a 'URL-to-Thumbnail' generator.
Visual Style: Use a dark-mode, minimalist aesthetic with a deep slate background. Incorporate glassmorphism elements (frosted glass cards), subtle neon accents, and a clean, 'developer-focused' layout inspired by the GSAP ScrollTrigger showcase.
Core Features to Visualize:

* Dynamic Gallery: A grid of site previews with visible metadata (title, URL, and platform icons).
* Control Panel: A floating or bottom-sheet configuration menu with sleek sliders for image quality and toggles for watermark/format (HTML, Markdown, Plain URL).
* Interaction Cues: Visual indicators for drag-and-drop handles and batch-upload progress bars.
* Typography: Use a clean sans-serif font (like Inter or Roboto Mono) with high-contrast hierarchy for readability.

Goal: The output should look like a premium, interactive tool found on a site like Vercel or Framer."

------------------------------
## Why this works better:

* Contextual Roleplay: Starting with "Senior UI/UX Designer" tells the AI to prioritize aesthetics, not just functional code.
* Visual Keywords: Using "glassmorphism," "slate background," and "high-fidelity" ensures the AI moves away from generic, boxy interfaces.
* Component-Based: It breaks the UI into "Header," "Gallery," and "Control Panel," which helps the AI structure the layout logically for a mobile screen.
* GSAP Reference: Mentioning "ScrollTrigger showcase" explicitly points the AI toward the specific high-end animation galleries you liked.

Pro-Tip: If you are using this prompt in an Image Generator (like DALL-E or Midjourney) to get a mockup, it's perfect. If you are using it in a Code Assistant (like Claude or GPT-4), it will generate the frontend code that actually looks like those premium examples.
Would you like me to help you write the actual CSS/HTML skeleton for that mobile control panel?

